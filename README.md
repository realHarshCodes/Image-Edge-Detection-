# Image-Edge-Detection-
A vector calculus-based image processing project that detects edges by computing the gradient of image intensity. It uses partial derivatives (∂I/∂x, ∂I/∂y) and gradient magnitude to highlight regions of sharp intensity change, revealing object boundaries in images.
